[project]
name = "spatial-photo-viewer"
version = "1.0.0"
description = "View and extract stereoscopic images from iPhone spatial photos"
requires-python = ">=3.8"
dependencies = [
    "pillow>=10.0.0",
    "pillow-heif>=0.13.0",
]

[project.scripts]
spatial-viewer = "spatial_photo_viewer:main"
